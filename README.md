/*videos*/



repositorio ="https://github.com/mateuscsp/git_203386"

# publico

#1 - 4

git init - inicializar repositório do Git

#1 - 5

#add arquivos

git add .

#vincular o git#primeiro commit

git commit -m "Primeiro commit"

git config --global user.email "mateuscsp@live.com"

git config --global user.name "Mateuscsp"

#primeiro commit

git commit -m "Primeiro commit"

git branch -M main

#conectar

git remote add origin git@github.com:mateuscsp/git_alura_203386.git

#arrumar a key

#settings

#ssgh/keys

#add key

ssh-keygen -t ed25519 -C "mateuscsp@live.com"

#pasta .ssh

id_ed25519.pub

#copiar o conteudo da chave publica e colar no github (key)

#enviar

git push -u origin main

#2 - 1

#outras pessoas usarem

https://github.com/mateuscsp/git_alura_203386

clone com ssh

git clone git@github.com:mateuscsp/git_alura_203386.git

#2 - 2

#ver alteracoes

git status

git add .

git commit -m "Alterações feitas"

git push

#ver os commits

git log

#2 - 3

#outros enviar pra o git hub

ver os repositórios

git remote -v

git push origin main

erro de permissao

adicionando colaborador

settings

collaborators

add people

convite aceitar

git push origin main

#2 - 4

#trazer as modificações

git pull

#3 - 1

#integração git x vscode

3o icone

vai ter M na frente

Add o arquivo

colocar a mensagem

pode fazer mais de 1

realizar o comitt



configuar inicial global
git config --global user.name "Mateuscsp" git config --global user.email "mateuscsp@live.com"

puxar repositorio

git remote add origin git@github.com:mateuscsp/git_alura_203386.git git push -u origin main

Configuar no git settings

ssh/keys adiconar nova chava

pegar chave pasta .ssh

gerar a chave

ssh-keygen -t ed25519 "mateuscsp@live.com" enter enter

abrir chave privada

copiar e colar no gitg

C:\Users\mateu\OneDrive\Cursocientista\04_engenhariadadospyhtonspark\git_hub
